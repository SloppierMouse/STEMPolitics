# Used by scripts/fetch-news.mjs when running the news pipeline locally
# (e.g. `npm run fetch-news`). In CI, the scheduled GitHub Action reads the
# same key from the ANTHROPIC_API_KEY repo secret instead of this file.
ANTHROPIC_API_KEY=
